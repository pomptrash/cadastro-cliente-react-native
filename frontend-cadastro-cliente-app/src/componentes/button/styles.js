import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  button: {
    backgroundColor: "#fff",
    padding: 15,
    margin: 4,
    borderRadius: 8,
    borderWidth:1,
    width: "250",
    alignItems: "center",
    alignSelf: "center",
  },
  buttonText: {
    color: "#033",
    fontWeight: "bold",
  },
});
