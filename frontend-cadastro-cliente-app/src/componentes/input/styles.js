import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    padding: 20,
    gap: 10,
  },
  label: {
    color: "#033",
    fontSize: 16,
    fontWeight: "bold",
  },
  input: {
    backgroundColor: "#0321",
    color: "#000",
    fontSize: 14,
    padding: 8,
    borderRadius: 8,
    borderWidth: 2,
    borderColor: "#0321",
  },
});
